#include "main.h"

/**
 * mul - multiplies two integers (0 through 9).
 * @a: the first int.
 * @b: the second int.
 * Return: returns mul.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}

