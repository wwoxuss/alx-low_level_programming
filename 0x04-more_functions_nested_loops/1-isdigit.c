#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9).
 * @n: the number to be checked.
 * Return: returns 1 if n is a digit, otherwise 0.
 */
int _isdigit(int n)
{
	return (n >= '0' && n <= '9');
}
